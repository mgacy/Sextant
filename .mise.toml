[tools]
swiftlint = "0.63.1"

[settings]
experimental = true

[tasks.lint]
description = "Lint the code"
run = "swiftlint"

[tasks.lint-fix]
description = "Auto-fix linting issues"
run = "swiftlint --fix"
